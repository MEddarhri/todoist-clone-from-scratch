import React from 'react';
import { Navbar } from './Styles';
import Logo from 'shared/logo';

const index = () => {
  return (
    <Navbar>
      <Logo />
    </Navbar>
  );
};

export default index;
