import React from 'react';
import { Container, Icon, Spinner } from './Styles';

const index = () => {
  return (
    <Container>
      <Icon />
      <Spinner />
    </Container>
  );
};

export default index;
